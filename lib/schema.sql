CREATE TABLE Series (
        id INTEGER PRIMARY KEY,
                name TEXT,
                age INTEGER
            );
CREATE TABLE Series (
        id INTEGER PRIMARY KEY,
                name TEXT,
                age INTEGER
            );
CREATE TABLE Series (
        id INTEGER PRIMARY KEY,
                name TEXT,
                age INTEGER
            );
CREATE TABLE Series (
        id INTEGER PRIMARY KEY,
                name TEXT,
                age INTEGER
            );
